*.o
*.a
build/

# ESP-IDF leaves these in the example project when you run `idf.py build` in
# place. CI builds out of tree (-B), so it never makes any of them.
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
